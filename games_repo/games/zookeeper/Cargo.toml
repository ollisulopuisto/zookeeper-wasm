[package]
name = "zookeeper_wasm"
version = "26.4.7"
edition = "2021"

[dependencies]
macroquad = { version = "0.4", features = ["audio"] }
quad-rand = "0.2"
serde = { version = "1.0", features = ["derive"] }
shared = { path = "../shared" }

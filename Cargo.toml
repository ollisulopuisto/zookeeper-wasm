[package]
name = "zookeeper_wasm"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
quad-rand = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
strip = true
